node_modules/

# DB 파일 (서버 실행시 자동 생성됨)
server/data/golf_ndvi.json
server/data/*.db

# 사용자 데이터 (로컬 영구 보존, git에는 포함 안 함)
# ※ user_data.json은 사용자 수정사항이 저장되는 핵심 파일입니다
# ※ 절대 삭제하지 마세요!
server/data/user_data.json
server/data/backups/

# 환경변수 (API 키 포함)
.env

# 테스트/임시 파일
test_*.js
planet_token.txt
*.log

# OS
.DS_Store
Thumbs.db

# 테스트 이미지
public/img/planet_*
public/img/s2_*
public/img/basemap_*

# 드론 영상 업로드 (용량이 크므로 git 제외)
public/uploads/drone/
server/data/drone_images.json

# 구역/온톨로지 데이터 (사용자 데이터)
server/data/zones.json

# LiDAR 업로드 데이터
public/uploads/lidar/
server/data/lidar_data.json
